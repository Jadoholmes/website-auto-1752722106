const express = require('express');const router = express.Router();const blogPosts = [{ id: 1, title: 'First Blog Post', content: 'This is the content of the first blog post.' },{ id: 2, title: 'Second Blog Post', content: 'This is the content of the second blog post.' }];router.get('/', (req, res) => {res.status(200).json(blogPosts);});router.get('/:id', (req, res) => {const post = blogPosts.find(p => p.id == req.params.id);if (post) {res.status(200).json(post);} else {res.status(404).json({ message: 'Post not found.' });}});module.exports = router;